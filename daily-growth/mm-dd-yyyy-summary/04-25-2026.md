# llnzy — Build Session Summary
## April 25, 2026

---

## What We Built

Completed the `effects_on_ui` theme flag implementation, then implemented in-place tab renaming (in-progress/needs refinement).

Later, completed Phase 7 of the code-quality cleanup roadmap: reduced suppressions, fixed easy Clippy warnings, validated with tests, and archived the now-stale roadmap under `daily-growth/roadmaps/old/`.

Also created the Sketch Pad roadmap and implemented the first working Sketch slice: sidebar navigation, canvas rendering, marker strokes, rectangles, text boxes, selection/delete basics, undo/redo/clear, JSON persistence, and input routing so Sketch does not leak input into the terminal.

Followed up by matching the Sketch workspace/canvas background to the active theme background and fixing text placement by allowing the canvas to receive normal click events as well as drag events.

Finished the remaining Sketch roadmap scope except import/export, which is intentionally out of scope: selected elements can now be moved, existing text can be edited by double-clicking with Select, rectangle drawing supports Shift square constraint and Alt-from-center behavior, and Sketch now renders through the real CRT/Retro shader path.

---

## Changes Made

### Completed: Per-Theme Shader Control (effects_on_ui flag)
- Added `effects_on_ui: bool` field to `EffectsConfig` in `config.rs`
- Set `effects_on_ui: false` on Buzz theme — CRT shaders now only affect terminal content, not sidebar/footer
- Set `effects_on_ui: true` on Synthwave theme — shaders apply to all UI elements
- Minimalist theme already had `effects_on_ui: true` (default)
- Updated `main.rs` to use `config.effects.effects_on_ui` when computing the effects_on_ui flag for RenderRequest
- Built and tested successfully — Buzz theme now renders clean sidebar without shader artifacts

### In-Progress: Tab Renaming (Not Working Yet)
- Added `name: Option<String>` field to Tab struct for custom names
- Updated tab creation to initialize with `name: None`
- Modified `tab_titles()` to use custom names when set, falling back to "Shell N"
- Added tab editing state to UiState:
  - `editing_tab: Option<usize>` — which tab index is being edited
  - `editing_tab_text: String` — text being typed
  - `saved_tab_name: Option<(usize, String)>` — pending name change
  - `last_tab_click: Option<(usize, Instant)>` — for double-click detection
- Added helper methods:
  - `start_editing_tab(tab_index, current_name)` — begin editing mode
  - `cancel_editing_tab()` — cancel without saving
  - `set_tab_context(tab_count, active_tab)` — update tab context before render
  - `take_saved_tab_name()` — retrieve pending name change
- Implemented double-click detection using time-based approach (300ms window)
- Added egui Area overlay with TextEdit when editing (positioned over the tab)
- Added Enter/Escape key handling
- Applied saved tab names in main.rs after render
- Builds successfully but feature not functioning — likely issue with input detection over GPU-rendered tab bar

### Completed: Phase 7 Code Quality Cleanup
- Removed retained-resource `#[allow(dead_code)]` fields from renderer resource structs
- Fixed Clippy warnings for manual range checks, manual div-ceil, needless borrows, collapsible `if`, `map_or` simplifications, doc list indentation, and default field reassignment
- Introduced small parameter/request structs for layout, bloom, CRT, and cursor rendering to reduce oversized function signatures
- Converted remaining intentional suppressions to `#[expect(..., reason = "...")]` so they are explicit and auditable
- Ran `cargo fmt`
- Ran `cargo clippy --all-targets --all-features` successfully with no warnings
- Ran `cargo test` successfully:
  - 193 library tests passed
  - 5 PTY round-trip tests passed
  - 45 terminal emulation tests passed
- Marked Phase 7 complete in the roadmap
- Moved `daily-growth/roadmaps/llnzy-code-quality-cleanup-roadmap.md` to `daily-growth/roadmaps/old/llnzy-code-quality-cleanup-roadmap.md`

### Completed: Sketch Pad First Slice
- Created `daily-growth/roadmaps/llnzy-sketch-pad-roadmap.md`
- Added `src/sketch.rs` for Sketch document/model state, draft actions, undo/redo stacks, hit testing, and JSON persistence
- Added `ActiveView::Sketch` and a `Sketch` sidebar button
- Added a full Sketch workspace with compact toolbar and canvas
- Implemented marker/freehand drawing with color swatches and stroke width
- Implemented rectangle creation with drag preview and tiny-rectangle filtering
- Implemented text box placement with focused editor overlay, Done/Cancel controls, and Cmd+Enter commit
- Implemented selection/delete basics with hit testing and selection outlines
- Added undo, redo, and clear toolbar actions plus Cmd+Z/Cmd+Shift+Z/Delete/Escape handling while Sketch is active
- Persisted the scratch sketch to `dirs::config_dir()/llnzy/sketches/scratch.json`
- Added terminal input capture for Sketch so mouse/text/key events do not fall through to the shell
- Added unit tests for Sketch model actions, hit testing, undo/redo, and serialization
- Ran `cargo fmt`, `cargo test`, and `cargo clippy --all-targets --all-features` successfully

### Completed: Sketch Pad Follow-Ups
- Changed the Sketch panel and canvas background to use `config.colors.background` from the active theme
- Changed the Sketch canvas input sensing from drag-only to click-and-drag so the Text tool can place text boxes on normal clicks
- Verified the follow-up changes with `cargo fmt` and `cargo check`

### Completed: Sketch Pad Roadmap Follow-Through
- Added drag-to-move behavior for selected Sketch elements
- Added double-click editing for existing text boxes while the Select tool is active
- Added rectangle modifier behavior:
  - Shift constrains drag rectangles to squares
  - Alt draws rectangles from the drag origin as center
- Kept Sketch import/export out of scope per product direction
- Routed Sketch through the renderer scene texture before post-processing so the existing CRT/bloom/background shader stack affects the Sketch workspace
- Removed the earlier painter-based CRT approximation to avoid double-applying scanlines/chromatic effects
- Added model tests for square rectangles, center-origin rectangles, text editing, and selected element movement
- Ran `cargo fmt`
- Ran `cargo test` successfully:
  - 206 library tests passed
  - 5 PTY round-trip tests passed
  - 45 terminal emulation tests passed
- Ran `cargo clippy --all-targets --all-features` successfully
- Visually confirmed the real shader path in Sketch after opening the workspace with CRT/Retro effects

---

## Files Modified

| File | Changes |
|------|---------|
| `src/theme.rs` | Added `effects_on_ui: false` to Buzz, `effects_on_ui: true` to Synthwave |
| `src/config.rs` | Already had `effects_on_ui` field from previous session; fixed Clippy range check |
| `src/main.rs` | Used config.effects.effects_on_ui in effects flag; applied tab name changes from UI; simplified Clippy warnings; routed Sketch egui through the real shader path |
| `src/ui.rs` | Tab renaming state tracking, double-click detection, text input overlay; cleaned style initialization; added Sketch view/canvas/tools; matched Sketch background to theme; fixed click handling for text placement; added selected-element movement, double-click text editing, and rectangle modifiers |
| `src/renderer/mod.rs` | Clarified UI shader routing comments for shadered vs clean egui views |
| `src/lib.rs` | Exported the new `sketch` module |
| `src/sketch.rs` | Added Sketch document model, actions, persistence helpers, hit testing, undo/redo, selected movement, existing text editing, rectangle modifier support, and tests |
| `src/layout.rs` | Added `LayoutInputs` to reduce `ScreenLayout::compute` argument count |
| `src/renderer/background.rs` | Removed retained dead layout field and fixed needless borrow |
| `src/renderer/bloom.rs` | Added `BloomParams`, removed retained dead field, fixed doc indentation |
| `src/renderer/crt.rs` | Added `CrtParams` to reduce argument count |
| `src/renderer/cursor.rs` | Added `CursorDrawRequest`, simplified cursor movement check |
| `src/renderer/mod.rs` | Updated renderer call sites for new params/request structs |
| `src/renderer/particles.rs` | Removed retained dead buffer field and used `div_ceil` |
| `src/renderer/text.rs` | Converted remaining intentional Clippy suppressions to documented `#[expect]` annotations |
| `src/error_log.rs` | Converted intentional type-complexity suppression to documented `#[expect]` |
| `tests/pty_roundtrip.rs` | Removed no-longer-needed suppression |
| `daily-growth/roadmaps/llnzy-sketch-pad-roadmap.md` | Added and updated Sketch Pad implementation roadmap |
| `daily-growth/roadmaps/old/llnzy-code-quality-cleanup-roadmap.md` | Archived roadmap after marking Phase 7 complete |

---

## Known Issues

- Tab double-click detection not triggering reliably — input may not be reaching the interaction code
- Possible cause: egui input routing when on Shells view, or timing of click detection
- Text input overlay renders but doesn't receive focus/input properly

---

## What's Next

- Debug tab double-click detection (verify click events are reaching code)
- Consider alternative approach: maybe simple single-click button to start edit, or different input detection method
- Fix docs drift: built-in theme count and config path wording are out of sync with code
- Decide what to do with the blank Settings view
- Populate the new Settings view with configuration options
- Improve sidebar UX (icons, collapsible sections)
- Distribution (release binaries, .app bundle, Homebrew)
