# llnzy -- Build Session Summary
## May 2, 2026

---

## What We Built

Focused on the reliability and durability roadmap for the editor/terminal workbench.

The work moved through the first major hardening passes:

- Phase 1: split oversized UI surfaces into smaller owned modules.
- Phase 2: introduced typed command routing for editor, palette, footer, menu, and app-level keybinding paths.
- Phase 3: started state identity and session reliability by replacing code-file tab buffer indexes with stable buffer IDs and repairing last-session restore.

---

## Markdown Preview

Added a Markdown preview experience for `.md` files.

Completed:

- Source, preview, and split Markdown modes.
- Command palette entries for Markdown mode switching.
- Preview styling that reads more like a web/blog page.
- Left-aligned Markdown headings and page content.
- Matching gray page margins around the preview.
- Removed the unwanted `Cmd+Shift+V` shortcut.

---

## Settings Hotkey Legend

Added a hotkey legend to the Settings tab.

Completed:

- Added a `Shortcuts` section at the top of Settings.
- Added a `Show Hotkey Legend` / `Hide Hotkey Legend` toggle.
- Built the legend from command palette registrations where possible.
- Added editor-only shortcuts that are not command palette entries.
- Grouped shortcuts by Editor, Navigation, LSP, Markdown, Terminal, and System.

---

## Phase 1: UI Surface Split

Reduced the largest editor UI files by extracting focused modules.

Key extraction work:

- `editor_command.rs`
- `editor_cursor.rs`
- `editor_gutter.rs`
- `editor_inline_overlays.rs`
- `editor_input.rs`
- `editor_line_decorations.rs`
- `editor_lines.rs`
- `editor_minimap.rs`
- `editor_selection.rs`
- `editor_status.rs`
- `palette_command_dispatch.rs`
- `settings_hotkeys.rs`
- `sidebar_file_browser.rs`
- `sidebar_file_types.rs`
- `sidebar_finder.rs`

Primary size targets after the split:

- `src/ui/explorer_view.rs` is under the Phase 1 target.
- `src/ui/editor_view.rs` is under the Phase 1 target.
- `src/ui/sidebar_tree.rs` is under the Phase 1 target.

---

## Phase 2: Typed Command Model

Built out a real command model so multiple surfaces share the same behavior.

Completed:

- Added `EditorCommand` for editor-specific actions.
- Routed command palette editor actions through `EditorCommand`.
- Routed standard editor key actions through `EditorCommand`:
  - save
  - undo
  - redo
  - select all
  - cut
  - copy
  - paste
  - delete line
  - duplicate line
  - move line up/down
- Added app-level palette routing for workspace open, terminal creation, tab navigation, sidebar, FPS, and effects.
- Added `AppCommand::PickOpenProject`.
- Added `src/app/keybinding_commands.rs` to translate global keybindings into `AppCommand`.
- Routed global keybindings and macOS menu actions through typed app commands where practical.

Added command routing tests for:

- palette-to-editor command mapping
- app palette command routing
- key action edit routing
- previous-tab wrapping
- global keybinding-to-command behavior

---

## Phase 3: State Identity And Session Reliability

Started the state identity work with multiple parallel agents.

Completed:

- Added stable `BufferId` values to `EditorState`.
- Added editor registry helpers for resolving, switching, and closing buffers by `BufferId`.
- Added editor registry helpers for path lookup, path update, and dirty-buffer identity reporting.
- Updated code-file tabs to store `BufferId` instead of editor buffer indexes.
- Updated UI pane metadata and `AppCommand::OpenCodeFile` to carry `BufferId`.
- Moved async parse result application from shifting view indexes to stable buffer IDs.
- Reworked close-tab, modified-tab, save-for-close, and tab-sync paths to resolve buffers by ID.
- Added `AppCommand::RemapCodeFilePath`.
- Remapped open buffers and code-file tabs after sidebar rename.
- Kept file move remapping on the same identity-safe path.
- Blocked `Close Other Tabs` and `Close Tabs To Right` when those actions would remove a dirty code-file tab from the workspace tab bar.
- Repaired last-session restore so startup no longer just deletes the session snapshot.
- Extracted session restore decision-making into a pure restore planner.
- Added backward-compatible `SessionSnapshot.active_tab`.
- Saved and restored active tab when possible.
- Skipped missing restored files safely.
- Fell back to Home when a saved session has no usable tabs.
- Split runtime drag/drop command execution and file path remapping into `src/runtime/drag_drop.rs`, reducing `src/runtime/commands.rs` to 524 lines.
- Split save-prompt response handling into `src/runtime/save_prompt.rs`, bringing `src/runtime/commands.rs` down to 445 lines.
- Added Git UI async request identity guards so stale refresh/detail results cannot overwrite the current repository or selected commit state.
- Added `BufferId` identity guards to buffer-local LSP pending requests and made delayed formatting responses mutate and notify the original request buffer instead of the active buffer at response time.
- Added restore skip status summaries for missing projects/files.
- Added `EditorState::active_buffer_view()` and moved LSP request creation onto that active buffer identity helper.
- Added a pure code-file tab path remap helper and focused tests for path-based and `BufferId`-based tab remapping.
- Marked Phase 3 complete in the reliability roadmap, with remaining external file lifecycle work moving to Phase 4.
- Started Phase 4 by adding a pure external file-event planner for clean external modifications, dirty external modifications, and deleted files.
- Hardened external reload prompts so they target stable `BufferId` values instead of shifting buffer indexes.
- Added stale external prompt protection so a reload prompt cannot apply after the buffer has been remapped to a different path.
- Tightened deleted-file path matching so missing paths are compared by exact path instead of collapsing failed canonicalization.
- Added sidebar rename/delete lifecycle guards for open files.
- Blocked sidebar rename/delete of dirty open files until they are saved or closed.
- Blocked sidebar folder rename/delete when open child buffers would otherwise be left on stale paths.
- Centralized lifecycle path comparison in `src/path_utils.rs` for external file events and sidebar actions.
- Added file watcher classification for modified, deleted, and detectable external rename/move events.
- Added explicit external file lifecycle states for clean/current, clean/dirty external changes, clean/dirty deletes, and clean/dirty moved-on-disk.
- Added moved-file prompts that can use the existing open-file remap path when notify provides a destination.
- Hardened normal editor save and save-for-close failures so dirty buffers stay dirty and durable status/prompt/log messages are shown.
- Added testable drag/drop move guards for open clean files, open dirty files, and unrelated dirty files.
- Extracted external prompt response planning for reload, moved-file remap, stale prompt rejection, and deleted/moved keep actions.
- Started Phase 5 by fixing wrapped mouse hit testing so clicks past text on a visual wrap row clamp to that row instead of jumping deeper into the logical line.
- Added editor hit-testing coverage for wrapped rows, scrolled wrapped rows, horizontal scroll, folded visible-line mapping, and cursor wrap-row lookup at line end.
- Hardened cursor behavior across empty files, long lines, document start/end selection, page movement, clamping, duplicate extra cursors, and non-ASCII occurrence selection.
- Hardened pointer input boundaries for gutter fold controls, negative y positions, scrolled-out rows, minimap exclusion, and click suppression after gutter fold handling.
- Improved Markdown preview parsing/rendering for structured tables, nested lists, local image paths, and fenced code language labels.
- Completed Phase 5 of the reliability roadmap.
- Started Phase 6 by splitting terminal durability work across PTY lifecycle coverage, terminal parser/emulation durability, and input routing/keybinding boundaries.
- Added the Phase 6 manual terminal stress checklist covering long output, resize during output, tab switching during output, multi-line paste, terminal file-path opening, kill/restart, and shortcut leakage checks.
- Expanded PTY lifecycle coverage for process exit/restart clarity, kill behavior, resize during streaming output, large paste integrity, and OSC title propagation through a real PTY.
- Hardened terminal emulation by clamping zero-sized terminal requests to `1x1` and adding parser coverage for isolated scroll regions, split OSC titles, title stack push/pop, multi-line payload rendering, shrink/grow resize preservation, and split SGR styling across wrapped output.
- Hardened terminal input routing by adding an explicit plain text key/paste/ignore route, narrowing `encode_key` to single-key plain text, testing modified-text boundaries, fixing zero-tab next-tab command handling, and normalizing Stacker CRLF/CR paste input.
- Created `daily-growth/roadmaps/reliability-manual-testing-05-02-2026.md` to track remaining manual reliability checks separately from the implementation roadmap.
- Completed OSC 7/CWD surfacing with a public terminal working-directory event, streaming `file://` OSC 7 parsing, percent-decoded path extraction, direct `Session.cwd` updates, and real PTY round-trip coverage.
- Hardened runtime terminal lifecycle behavior with explicit kill/restart rejection messages, already-exited kill suppression, restart planning that preserves cwd/custom tab names, and terminal exit message tests.
- Finished Phase 6 with extra input stress coverage for empty keybinding rejection, literal plus keybindings, Shift text routing, modified multi-line suppression, and split bracketed-paste control sequences.
- Completed Phase 6 of the reliability roadmap.
- Started Phase 7 by splitting LSP lifecycle hardening across document lifecycle state, manager root/server health, and editor/UI stale async response guards.
- Completed Phase 7 by adding a centralized LSP document store for open/change/save/close/move lifecycle state and versioning.
- Hardened LSP manager root/server lifecycle behavior for root changes, stopped-client retry, structured status labels, unavailable reasons, registry lookup states, and diagnostics clear/remap.
- Wired moved open files through `LspManager::did_move` so LSP document identity closes the old URI, opens the new URI, and remaps diagnostics.
- Hardened editor LSP stale-response handling across hover, completion, definition, signature help, references, inlay hints, code lenses, code actions, document symbols, rename, and formatting.
- Completed Phase 7 of the reliability roadmap.

Added tests for:

- stable buffer IDs after index shifts
- switching by buffer ID after index shifts
- path lookup/update through the buffer registry
- dirty-buffer ID reporting
- async parse still attaching to the correct buffer
- session snapshot round-trip with active tab
- old session snapshots without active tab
- restore planner behavior for normal sessions, missing projects, missing files, active-tab remapping, and Home fallback
- Git UI refresh/detail request identity and stale detail discard behavior
- LSP request identity, delayed formatting after active-buffer switches, and closed-buffer formatting discard behavior
- restore skip status summaries
- active buffer identity helper behavior
- code-file tab path remapping by old path and by `BufferId`
- external file-event planning for clean modified, dirty modified, and deleted files
- external reload prompt targeting after buffer index shifts
- stale external prompt rejection after buffer path remapping
- missing-path matching for deleted file events
- sidebar dirty open file lifecycle blocking
- sidebar clean exact file rename allowance
- sidebar folder lifecycle blocking for clean and dirty open child buffers
- path utility coverage for distinct missing paths and existing child containment
- file watcher classification for modified, deleted, and moved files
- external file lifecycle modeling for clean/current, modified, deleted, and moved states
- editor save failure preserving dirty state and status
- save-for-close failure preserving dirty state and formatting durable errors
- drag/drop move blocking for open dirty files while allowing clean or unrelated files
- external prompt response planning for reload, moved-file remap, stale rejection, and keep actions
- editor mouse hit testing under word wrap, horizontal scroll, and folded visible-line mappings
- cursor movement and selection under empty buffers, long lines, page movement, document boundaries, duplicate cursor clamping, and non-ASCII occurrence selection
- pointer input hit boundaries around gutter folds, minimap exclusion, negative y positions, scrolled-out rows, and handled gutter clicks
- Markdown preview tables, nested lists, local images, relative image path resolution, and fenced code language labels
- PTY lifecycle behavior for exit/restart, kill/disconnect, streaming resize, large paste integrity, and OSC title propagation
- terminal emulation behavior for zero-size clamping, scroll-region isolation, split OSC titles, title stack push/pop, multi-line payload rendering, resize shrink/grow preservation, and split SGR styling
- terminal input routing and command boundaries for plain text key/paste routing, modified text suppression, keybinding matching, zero-tab navigation, and Stacker CRLF/CR paste normalization
- OSC 7 working-directory parsing, session cwd updates, and real PTY round-trip behavior
- runtime terminal kill/restart lifecycle planning, rejection messages, and process exit log formatting
- keybinding parser edge cases for empty bindings and literal plus bindings
- bracketed paste mode parsing across split control sequence bytes
- LSP document lifecycle duplicate open/change/save/close/move behavior
- LSP manager root changes, stopped-client retry planning, structured lifecycle labels, registry lookup states, and diagnostics clear/remap
- editor-layer stale/closed-buffer LSP responses across hover, completion, definition, signature help, references, inlay hints, code lenses, code actions, document symbols, rename, and formatting

---

## Validation

Validation completed during the session:

```bash
cargo fmt
cargo check
cargo test
git diff --check
```

The full test suite passed after completing the multi-agent Phase 7 LSP lifecycle batch:

- 512 library tests passed
- 12 runtime/app unit tests passed
- 11 PTY tests passed
- 54 terminal emulation tests passed
- doc tests passed

---

## Notes

The roadmap document was updated alongside the code:

- `daily-growth/roadmaps/llnzy-reliability-durability-roadmap-05-01-2026.md`

The broad Phase 4 reliability batch and Phase 5 editor correctness batch were pushed to `main` earlier in the session. Phase 6 terminal durability and Phase 7 LSP lifecycle hardening are complete and validated locally.
