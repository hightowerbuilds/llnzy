# 09-06-2026 Summary

## Breadcrumbs

Top-down scan of today's work, in order.

**Codebase orientation**
- Full read of the repo: README, Cargo.toml, CI, docs (architecture,
  development, quality policy), module map (~47k lines), git history.
- Flagged pre-existing issues: `docs/development.md` contradicted CI's
  `linux-check` job; `roadmaps/style-demo.md` sat untracked.

**Academy paper trail**
- `daily-growth/roadmaps/future/code-academy.md`: platform roadmap —
  markdown courses with `course.toml`, captured-subprocess checks
  (exact/contains/exit_code), local JSON progress, Academy as a
  first-class workspace surface, course lint in CI, non-goals fence.
- Two launch course roadmaps (delegated, then verified on disk):
  `academy-course-rust.md` (18 lessons + `sift` final project) and
  `academy-course-javascript-typescript.md` (two acts, `ledger` final
  project; tsx offline scheme: one-time student install + walk-up
  `node_modules` resolution with `npx --no-install`).

**Pre-Academy cleanup + Settings rework**
- Fixed the development.md/CI contradiction; filed style-demo.md into
  daily-growth.
- Settings rebuilt into three tabs: Appearances (all visual controls in
  TERMINAL / EDITOR / EDITOR BEHAVIOR / APP sections), Terminal
  (behavior-only; new Scrollback Lines setting, 1k–100k), Advanced
  (joined-tab limit + error log). Standalone Appearances surface keeps
  its theme column, one scroll.

**Academy surface shipped**
- New `WorkspaceSurface::Academy`: course picker with two launch cards,
  module preview panel, focus routing, workspace recovery support
  (old snapshots still load via Unknown fallback), Code Academy menu
  item.
- Home "Open Course" button, footer "Courses" button; sidebar New
  Course button added then removed on owner call (sidebar stays
  workspace-scoped).
- Home COURSE PROGRESS section: per-course rows with language insignia
  (JS yellow square, official Rust gear — embedded via include_bytes,
  license notes in `assets/academy/README.md`), completed/total +
  percent, accent progress bar; click opens Academy on that course.
- `src/academy_progress.rs`: persisted store (`<data_dir>/academy/
  progress.json`, atomic writes, corrupt→empty, sanitizes unknown/
  out-of-range entries), 6 unit tests. Single write path:
  `record_lesson_complete`.

**Rust course aligned to the book**
- Research: TRPL 3rd ed. (No Starch; print 2025; Rust 2024 edition) by
  Klabnik, Nichols, Krycho. Ch 1–13 verified from publisher ToC; new
  ch 17 async (Krycho), Miri coverage, 2024 idioms; three project
  chapters. Ch 18–19 titles flagged for print-ToC verification (2nd vs
  3rd ed. renumbering).
- `academy-rust-trpl.md`: module map (21 modules ≈ 50 lessons, two
  capstones — `sift` at ch 12 position, web server final), teaching
  app phases, license line (original prose only, cross-referenced to
  chapters — never copied book text), risks (drift, volume, CI
  compile-time budgets).

**Phase A begun: format + first content**
- `src/academy/` (pure, no GPUI): manifest + lesson frontmatter types
  (strict, `deny_unknown_fields`), path-precise `AcademyError`,
  `CourseLibrary` with deterministic tree signature and
  reload-if-changed that keeps the last-good course on a broken edit.
  20 tests including a strict-load test over the bundled course.
- Serde surface matches authored files: `[[exercise]]` tables via
  field rename, `exit_code` mode via variant rename (both caught by
  the strict-load test, not by eye).
- `assets/academy/courses/rust/`: modules for book ch 1–3, lessons
  L00–L06, each a cargo starter/solution pair with deterministic
  checks (pinned LCG instead of rand; no stdin in checks). Agent
  verified solutions pass / starters fail against real cargo before
  delivery; the strict-load test keeps it that way in CI.

## Delegation notes

- Course roadmap authoring and lesson-content authoring delegated
  cleanly (fixed file-writing contracts, tool budgets). One roadmap
  agent claimed success without writing (re-ran with "not done until
  the file is on disk" instructions); one parser agent burned its
  budget on reasoning and wrote nothing (wrote it by hand instead);
  one transport error arrived *after* a successful write (verified
  on disk, no redo needed). Lesson: always verify artifacts on disk
  regardless of the agent's reported status.

## Verification

- `cargo fmt --check`, `cargo clippy --all-targets --all-features
  -- -D warnings`, `cargo test --all-targets --all-features` green
  at each commit; final state: 542 lib tests (20 academy, 6 progress),
  all integration suites pass.
- Manual smoke deferred per `docs/manual-smoke-tests.md`: Settings
  three-tab layout, Home progress rows + logos, Academy picker,
  footer Courses button (all shipped in this session's commits).

## Next

- Phase A remainder: lesson reader in the Academy surface (real lesson
  list + markdown body), exercise materialization into
  `data_dir/academy/`, check runner (120s/90s budgets already decided),
  check-pass → `record_lesson_complete` wiring.
- Then Phase B: author TRPL ch 4–6 lessons (ownership pacing split is
  the one new-design item), and verify ch 18–19 titles against the
  print ToC before Phase D.
