# 07-01-2026 Summary

## Breadcrumbs

Top-down scan of today's work, in order.

**Codebase orientation**
- Re-read the repository layout, README, and `docs/architecture.md` to confirm
  the current module map: terminal, editor, LSP, workspace shell, Stacker,
  Sketch, effects, and the config/theme/platform support layers.
- Confirmed the working tree was clean before changing files.

**Shortlist roadmap**
- Created a top-level `roadmaps/` folder with `shortlist.md`, a checkbox list
  of near-term fixes grouped by surface: editor (word wrap, markdown preview
  styles), tabs/panes (2nd joined tab color, "swap side" label), terminal
  (Claude Code scroll problems), workspace (desktop menu, sidebar/explorer
  immediate file appearance), and rendering (shader options).

**Quick wins: joined tabs**
- Renamed the joined-tab swap action from "Swap Tabs" to "Swap Side" in all
  three user-visible places: tab context menu (`gpui_workspace/tabs.rs`),
  desktop menu bar (`gpui_workspace.rs`), and command palette
  (`gpui_workspace/command_palette.rs`).
- Gave the 2nd+ member of a joined tab group a distinct border color. Added a
  `joined_secondary` palette field (`0x6ab8ff` dark, `0x5f7fb0` light); the
  first member keeps the queue green border.
- Added `GpuiTabs::joined_member_index` and passed the member index into the
  tab renderer instead of the previous `is_joined` bool, so border color
  follows position and updates when tabs swap sides.

**Sizing for later**
- Sized the sidebar/explorer immediate-file-appearance fix: the explorer has
  no filesystem watching today (only git uses `notify`), so the fix needs a
  watcher wired into the sidebar with debounce and expansion-state
  preservation. Deferred to a focused session.

## Verification

- `cargo check`
- `cargo test --lib` (649 passed, 0 failed)
- `cargo clippy` (clean)
