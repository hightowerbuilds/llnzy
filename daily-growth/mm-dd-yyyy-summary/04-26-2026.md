# llnzy — Build Session Summary
## April 26, 2026

---

## What We Built

Designed and implemented a full source code editor with LSP support inside llnzy. This session covered a comprehensive codebase review, a 13-phase roadmap, and the complete implementation of **Phases 1-12** plus a **major UI restructure** -- from zero to a multi-buffer text editor with tree-sitter syntax highlighting (11 languages), full LSP client, diagnostics, completion, hover, go-to-def, formatting, rename, code actions, symbols, terminal split, click-to-file, file tree, fuzzy finder, command palette, minimap, breadcrumbs, performance guards, **Home screen with Open Project + Recent Projects**, **footer navigation bar**, **sidebar file tree with bumper**, and **classic code editor layout** (sidebar = project tree, main area = editor). Deferred items collected in `leftovers-code-editor.md`.

---

## Phase 0: Codebase Review & Roadmap

Read all 35 source files (~22k LoC) to assess the current state. Produced a detailed roadmap at `daily-growth/roadmaps/llnzy-source-editor-roadmap.md` covering 4 arcs and 13 phases:

- **Arc I -- Editor Core** (Phases 1-4): Text buffer, rendering, editing operations, multi-buffer
- **Arc II -- Intelligence** (Phases 5-8): Tree-sitter syntax highlighting, LSP client, LSP features
- **Arc III -- Workflow** (Phases 9-11): File management, terminal integration, productivity UX
- **Arc IV -- Polish** (Phases 12-13): Performance optimization, distribution

Estimated ~20-29k new LoC to reach full LSP editor capability.

---

## Phase 1: Text Buffer & Document Model -- COMPLETE

### `src/editor/buffer.rs` (971 lines, 32 tests)
- `Buffer` struct wrapping `ropey::Rope` for O(log n) insertions and deletions
- `insert`, `insert_char`, `delete`, `replace` -- all record undo history
- Line ending detection (LF/CRLF) with preservation on save
- Indent style auto-detection (tabs vs N-spaces) from file content
- Atomic file saves (write to temp file, rename) to prevent corruption
- Position <-> char index bidirectional conversion
- Line operations: `delete_line`, `move_line_up`, `move_line_down`, `duplicate_line`
- Block operations: `indent_lines`, `dedent_lines`, `text_range`
- File I/O: `from_file` with encoding detection, `save` / `save_to` with CRLF restoration

### `src/editor/cursor.rs` (589 lines, 21 tests)
- `EditorCursor` with line + column position, optional selection anchor, desired column
- Unicode-aware movement via `unicode-segmentation` (grapheme clusters)
- Arrow keys: right/left (character), up/down (line with desired-col preservation)
- Word movement: Alt+arrow jumps word boundaries (alphanumeric vs punctuation vs whitespace)
- Smart Home: toggles between first non-whitespace and column 0
- End, Page Up/Down, document start/end
- Selection: Shift+movement extends, plain movement clears
- Word select (double-click), line select (triple-click), select all
- Go to line (1-indexed), clamp to valid bounds

### `src/editor/history.rs` (287 lines, 11 tests)
- `EditOp` captures start/end positions and old/new text for any edit
- Linear undo/redo stacks with configurable depth (default 1000)
- Coalescing: rapid keystrokes (<800ms, adjacent, same-line) merge into one undo entry
- Newlines break coalescing groups (each Enter gets its own undo)
- Saved-state tracking: `mark_saved()` / `is_at_saved()` for modified indicators

### `src/editor/mod.rs` (166 lines)
- `EditorState`: manages `Vec<Buffer>` + `Vec<BufferView>` + `SyntaxEngine` with active index
- `BufferView`: per-buffer cursor, scroll offsets, language ID, tree-sitter `Tree`, dirty flag
- `open(path)`: opens file, detects language, parses initial syntax tree, or switches to existing tab
- `reparse_active()`: incremental tree-sitter re-parse when `tree_dirty` is set
- `close(idx)`, `switch_to(idx)`, `tab_info()` for UI

---

## Phase 2: Editor View & Rendering -- COMPLETE

### `src/ui/explorer_view.rs` (965 lines)
- Custom-painted editor using egui's `allocate_painter` API
- Line number gutter (auto-width based on line count, current line highlighted)
- Current line background highlight
- Blinking beam cursor (500ms cycle) clipped to text area
- Selection rendering with translucent blue highlight
- Vertical scrolling with cursor tracking and scrollbar hint
- Horizontal scrolling with 4-column margin, text clipping to viewport
- Mouse click to position cursor
- Status bar: `Ln N, Col N | M lines | Spaces: 4 | Modified/Saved`

---

## Phase 3: Core Editing & Multi-Buffer -- COMPLETE

### Clipboard (Cmd+C/X/V)
- Copy: selection -> clipboard; no selection -> copy whole line
- Cut: selection -> clipboard + delete; no selection -> cut whole line
- Paste: insert at cursor, replacing selection if active
- Two-way clipboard bridge: editor writes `clipboard_out`, main loop reads arboard and supplies `clipboard_in`

### Line Operations
- Cmd+Shift+K: delete entire line
- Cmd+Shift+D: duplicate line below
- Alt+Up/Down: move line up/down (swap with adjacent line)

### Indent/Dedent
- Tab: insert indent at cursor (respects file's detected indent style)
- Tab with selection: indent all selected lines by one level
- Shift+Tab: dedent (selection or single line)

### Auto-Pairing
- Auto-close on open bracket: `()`, `[]`, `{}`, `""`, `''`, backtick pairs
- Skip-over: typing a closing bracket jumps past it if it matches
- Pair deletion: backspace between empty pair deletes both characters
- Context-aware: only auto-pairs when next char is whitespace/closing/EOL

### Smart Enter
- Auto-indent: copies leading whitespace from current line
- Extra indent after `{`, `(`, `[` (detects trailing open bracket before cursor)

### Multi-Buffer Tabs
- Tab bar rendered above editor with file names
- Modified indicator (`*`) on unsaved tabs
- Click tab to switch, middle/right-click to close
- Active tab highlighted in blue, inactive in dark grey
- Per-buffer cursor + scroll state preserved when switching tabs
- "< Files" button returns to directory browser without closing buffers

---

## Phase 5: Tree-Sitter Syntax Highlighting -- COMPLETE

### `src/editor/syntax.rs` (550 lines, 6 tests)
- `SyntaxEngine` wrapping tree-sitter `Parser` + compiled `Query` per language
- `detect_language(path)` maps file extensions to language IDs
- `parse(lang_id, source)` -- full parse on file open
- `reparse(lang_id, source, old_tree)` -- incremental re-parse after edits (O(edit-size))
- `highlights_for_range(lang_id, tree, source, start_line, end_line)` -- returns colored spans for the visible viewport only
- Byte-range restriction on `QueryCursor` for performance (only queries visible lines)
- `StreamingIterator`-based query match iteration (tree-sitter 0.26 API)

### 16 Highlight Groups
Keyword, Type, Function, Variable, String, Number, Comment, Operator, Punctuation, Constant, Attribute, Tag, Property, Escape, Label, Module -- each mapped to an RGB color (One Dark-inspired palette).

### Supported Languages (11)
| Language | Grammar Crate | Version |
|----------|--------------|---------|
| Rust | `tree-sitter-rust` | 0.24 |
| JavaScript | `tree-sitter-javascript` | 0.25 |
| TypeScript | `tree-sitter-typescript` | 0.23 |
| TSX | `tree-sitter-typescript` | 0.23 |
| Python | `tree-sitter-python` | 0.25 |
| Go | `tree-sitter-go` | 0.25 |
| C | `tree-sitter-c` | 0.24 |
| JSON | `tree-sitter-json` | 0.24 |
| HTML | `tree-sitter-html` | 0.23 |
| CSS | `tree-sitter-css` | 0.25 |
| Bash | `tree-sitter-bash` | 0.25 |

TOML grammar (`tree-sitter-toml` 0.20) is incompatible with tree-sitter 0.26 -- query and detection code is ready, waiting for upstream update.

### Editor Integration
- `EditorState.open()` detects the language from file extension and parses the initial syntax tree on open
- After any edit that changes content length, `view.tree_dirty` is flagged
- Before each render frame, `reparse_active()` incrementally updates the tree using the old tree as a reference
- The editor renderer fetches highlight spans for only the visible line range
- Text rendering batches consecutive characters with the same highlight color into single draw calls
- Files with no recognized grammar render as plain white text (graceful fallback)

### Highlight Query Design
Each language has a compact, hand-written query using tree-sitter's S-expression pattern syntax. Queries target the most visually impactful nodes: keywords, string/number literals, comments, function names, type identifiers, attributes, properties, operators, and punctuation. Capture names map to `HighlightGroup` via prefix matching (e.g. `keyword.return` -> `Keyword`).

The Rust query uses `concat!()` of string literals instead of `r#"..."#` to avoid conflicts with tree-sitter query `"#"` patterns. Other languages use standard raw strings.

---

## Phase 6: LSP Client Foundation -- COMPLETE

### `src/lsp/transport.rs` (220 lines)
- Content-Length framed JSON-RPC over child process stdio (tokio async)
- Async reader task parses headers, reads body, dispatches messages
- Request/response matching via `AtomicI64` ID counter + `oneshot` channels
- Server notifications forwarded to `mpsc::UnboundedReceiver`
- `EventLoopProxy::send_event(LspMessage)` wakes the main thread on incoming messages

### `src/lsp/client.rs` (385 lines)
- Full `initialize` handshake with client capabilities (text sync, completion, hover, diagnostics, definition, references)
- Document lifecycle: `did_open`, `did_change` (full sync), `did_save`, `did_close`
- LSP requests: `hover`, `definition`, `completion`
- Graceful `shutdown` + `exit` sequence
- URI helpers: `path_to_uri` / `uri_to_path` for file:// conversions

### `src/lsp/registry.rs` (85 lines)
- 11 language server configs: rust-analyzer, typescript-language-server, pyright, gopls, clangd, bash-language-server, vscode-html/css/json-language-server
- `is_available(command)` checks PATH via `which`
- `find_server(lang_id)` returns config only if server is installed

### `src/lsp/mod.rs` (229 lines)
- `LspManager`: owns tokio `Runtime`, manages multiple `LspClient` instances
- `ensure_server(lang_id)`: starts server on first use (blocking init)
- Document sync methods delegate to the right client by lang_id
- `handle_notification`: dispatches `publishDiagnostics` to `HashMap<PathBuf, Vec<FileDiagnostic>>`
- `detect_root(path)`: walks up to find `.git`, `Cargo.toml`, `package.json`, etc.

### Editor Integration
- `EditorViewState.init_lsp(proxy)`: lazily creates the manager from the event loop proxy
- `EditorViewState.open_file(path)`: detects project root, starts server, sends `didOpen`
- Content-length change detection after key handling triggers `didChange`
- Save detection (was_modified -> !is_modified) triggers `didSave`

---

## Phase 7.1: Diagnostics Rendering -- COMPLETE

- Squiggly underlines rendered as alternating line segments: red (error), yellow (warning), blue (info), grey (hint)
- Gutter markers: `E` / `W` / `i` / `.` in matching severity colors
- Diagnostics fetched from `LspManager` per file each frame
- Clipped to visible viewport, positioned by line/column from LSP range

**Full pipeline**: open file -> detect language -> find server on PATH -> spawn -> initialize -> didOpen -> receive publishDiagnostics -> render underlines. When a supported server like `rust-analyzer` is installed, diagnostics appear automatically.

---

## New Dependencies

| Crate | Phase | Purpose |
|-------|-------|---------|
| `ropey = "1"` | 1 | Rope data structure for efficient text editing |
| `unicode-segmentation = "1"` | 1 | Grapheme cluster iteration for cursor movement |
| `tree-sitter = "0.26"` | 5 | Incremental parsing library |
| `streaming-iterator = "0.1"` | 5 | Required for tree-sitter 0.26 query match iteration |
| `tree-sitter-rust = "0.24"` | 5 | Rust grammar |
| `tree-sitter-javascript = "0.25"` | 5 | JavaScript grammar |
| `tree-sitter-typescript = "0.23"` | 5 | TypeScript + TSX grammars |
| `tree-sitter-python = "0.25"` | 5 | Python grammar |
| `tree-sitter-go = "0.25"` | 5 | Go grammar |
| `tree-sitter-c = "0.24"` | 5 | C grammar |
| `tree-sitter-json = "0.24"` | 5 | JSON grammar |
| `tree-sitter-html = "0.23"` | 5 | HTML grammar |
| `tree-sitter-css = "0.25"` | 5 | CSS grammar |
| `tree-sitter-bash = "0.25"` | 5 | Bash grammar |
| `lsp-types = "0.97"` | 6 | LSP protocol type definitions |
| `tokio` (multi-thread, process, io, sync, time) | 6 | Async runtime for LSP child process communication |

---

## Phase 7.2: Auto-Completion -- COMPLETE

- Ctrl+Space triggers `textDocument/completion` at cursor position
- Scrollable popup rendered below cursor with up to 20 filtered items
- Kind icons per `CompletionItemKind`: `f` (function/method), `v` (variable), `S` (struct/class), `M` (module), `k` (keyword), `p` (field/property), `C` (constant), `e` (enum member), `I` (interface), `T` (type parameter)
- Up/Down arrow navigation, Tab/Enter accepts (inserts completion text replacing filter), Escape dismisses
- Fuzzy filter: completion list narrows as user types after trigger point
- Detail text shown dimmed on the right side of each item
- Selected item highlighted in blue
- Cloned completion snapshot pattern avoids borrow conflicts with mutable editor state

## Phase 7.3: Hover Information -- COMPLETE

- F1 triggers `textDocument/hover` at cursor position
- Tooltip rendered above cursor (or below if no room above)
- Dark background (#282A36) with border, monospace font, capped at 12 lines
- Supports `MarkedString`, `MarkedString[]`, and `MarkupContent` hover formats
- Dismissed automatically on edit

## Phase 7.4: Go-to-Definition -- COMPLETE

- F12 triggers `textDocument/definition` at cursor position
- Jumps to target file:line:col, opening a new tab if the file isn't already open
- Handles all three response types: `Location`, `Location[]`, `LocationLink[]`
- `KeyAction` return pattern: keymap signals LSP requests without owning the LSP manager -- clean separation between input handling and LSP orchestration

---

## Codebase Refactoring -- COMPLETE

Extracted the 1,102-line `explorer_view.rs` into three focused modules:
- `src/editor/keymap.rs` (381 lines) -- keyboard handling with `KeyAction` return type for LSP signals
- `src/ui/editor_view.rs` (446 lines) -- editor rendering (syntax, diagnostics, hover, completions, cursor, scrollbar)
- `src/ui/explorer_view.rs` (483 lines) -- thin router: file browser, image viewer, tab bar, LSP state management

No file in the editor or LSP modules exceeds 971 lines (buffer.rs, half of which is tests).

---

## Test Results

**276 tests passing**, zero warnings.

| Module | Tests |
|--------|-------|
| `editor::buffer` | 32 |
| `editor::cursor` | 21 |
| `editor::history` | 11 |
| `editor::syntax` | 6 |
| Existing modules | 206 |
| **Total** | **276** |

---

## Files Created

| File | Lines | Description |
|------|-------|-------------|
| `src/editor/mod.rs` | 167 | EditorState multi-buffer manager with SyntaxEngine + per-buffer tree-sitter Trees |
| `src/editor/buffer.rs` | 971 | Rope-backed document buffer with undo/redo, line ops, file I/O |
| `src/editor/cursor.rs` | 589 | Unicode-aware cursor with selection, word/line movement |
| `src/editor/history.rs` | 287 | Undo/redo system with edit coalescing |
| `src/editor/syntax.rs` | 550 | Tree-sitter syntax engine with 11 languages and One Dark color palette |
| `src/editor/keymap.rs` | 405 | Editor keyboard handling with KeyAction LSP signaling (format, rename, actions, symbols) |
| `src/lsp/mod.rs` | 470 | LSP manager with all request wrappers + FormatEdit, CodeAction, SymbolInfo types |
| `src/lsp/transport.rs` | 220 | JSON-RPC over stdio with async reader and request/response matching |
| `src/lsp/client.rs` | 466 | LSP client with all request methods (formatting, rename, code actions, document symbols) |
| `src/lsp/registry.rs` | 85 | Language server command registry with PATH detection |
| `src/ui/editor_view.rs` | 446 | Editor rendering: syntax highlighting, diagnostics, hover, completion popup |
| `daily-growth/roadmaps/llnzy-source-editor-roadmap.md` | 580 | Full 13-phase LSP editor roadmap |
| **Total new code** | **~5,320** | across 13 files (editor + lsp + ui modules) |

## Files Modified

| File | Changes |
|------|---------|
| `Cargo.toml` | Added `ropey`, `unicode-segmentation`, `tree-sitter`, `streaming-iterator`, 11 language grammar crates, `lsp-types`, `tokio` |
| `src/lib.rs` | Registered `editor` and `lsp` modules, added `UserEvent::LspMessage` |
| `src/explorer.rs` | Changed `FileContent::Text` from `String` to `Buffer`, increased file size limit to 10MB |
| `src/ui/explorer_view.rs` | Rewrote as thin router (661 lines): tab bar, file browser, image viewer, LSP state (hover/completion/goto/format/rename/actions/symbols) |
| `src/ui/mod.rs` | Added `EditorViewState` to `UiState`, registered `editor_view` module, clipboard bridge |
| `src/main.rs` | Supply clipboard content to editor, init LSP manager, handle `LspMessage` event |

---

## Architecture Decisions

1. **Ropey over raw String**: O(log n) insertions make the editor viable for large files without recopying the entire document on each keystroke
2. **Per-buffer view state**: Each open buffer retains its own cursor position, scroll offset, language ID, and tree-sitter parse tree -- switching tabs restores exactly where you were with full syntax highlighting
3. **egui painter for rendering**: Started with egui's custom painting API rather than the direct glyphon pipeline. Faster iteration on UI features; can migrate to glyphon later if performance demands it
4. **Clipboard bridge pattern**: Editor can't directly access `arboard` (it's owned by the main `App`), so clipboard flows through `clipboard_out`/`clipboard_in` fields that the main loop mediates
5. **Explorer -> EditorState routing**: Text files open through `EditorState` (multi-buffer tabs with syntax trees); images stay in `explorer.open_file` (single preview). Clean separation
6. **Incremental tree-sitter parsing**: On file open, the full document is parsed. After edits, only the changed regions are re-parsed using `reparse()` with the old tree -- this keeps syntax highlighting responsive even on large files
7. **Viewport-restricted highlight queries**: `QueryCursor::set_byte_range()` restricts tree-sitter queries to only the visible line range, avoiding O(file-size) work on every render frame
8. **Content-length change detection**: Compares `buf.len_chars()` before/after key handling to detect edits for both tree-sitter re-parse and LSP `didChange` -- simple, correct, zero-overhead when idle
9. **Synchronous LSP on tokio runtime**: `LspManager` owns a tokio `Runtime` and uses `block_on()` for LSP calls. This keeps the API synchronous from the editor's perspective while the transport reader runs async. Server notifications arrive via `EventLoopProxy` waking the main thread
10. **Lazy server startup**: Language servers are only spawned when the first file of that language is opened, not at app startup. `ensure_server()` checks PATH availability before attempting to spawn
11. **Project root detection**: Walks up from the opened file to find `.git`, `Cargo.toml`, `package.json`, etc. and sets the LSP workspace root accordingly
12. **KeyAction return pattern**: The keymap function returns a `KeyAction` struct signaling which LSP operations to perform (goto-def, hover, completion trigger/accept/dismiss/navigate). This keeps the keymap pure (no LSP dependency) while the host code in `explorer_view.rs` orchestrates the actual LSP calls
13. **Completion snapshot cloning**: The completion item list is cloned out of `EditorViewState` before rendering to avoid borrow conflicts between the immutable completion data and the mutable buffer/view references needed by the editor renderer

---

## Phase 8: Advanced LSP Features -- COMPLETE (8.1-8.4)

### Phase 8.1 -- Document Formatting
- **Cmd+Shift+F** sends `textDocument/formatting`
- Applies `TextEdit[]` in reverse position order to preserve offsets
- Status: "Formatted" / "No formatting changes"

### Phase 8.2 -- Rename Symbol
- **F2** opens rename input prefilled with word at cursor
- Sends `textDocument/rename`, applies `WorkspaceEdit` to current buffer in reverse order
- Status shows occurrence count

### Phase 8.3 -- Code Actions
- **Cmd+.** sends `textDocument/codeAction` for cursor/selection range
- Returns `CodeAction[]` / `Command[]`; popup state with selection index
- `apply_code_action()` applies workspace edits to current buffer

### Phase 8.4 -- Document Symbols
- **Cmd+Shift+O** sends `textDocument/documentSymbol`
- Handles flat (`SymbolInformation[]`) and nested (`DocumentSymbol[]`) responses
- Nested symbols flattened recursively via `flatten_symbols()`
- Popup state with filter string and selection

### LSP Infrastructure Additions
- **`client.rs`**: Added `formatting()`, `rename()`, `code_actions()`, `document_symbols()` async methods
- **`mod.rs`**: Added `format()`, `rename()`, `code_actions()`, `document_symbols()` blocking wrappers + `FormatEdit`, `CodeAction`, `SymbolInfo` types + `parse_workspace_edit()` helper
- **`keymap.rs`**: Added `format_document`, `rename_symbol`, `code_actions`, `document_symbols` to `KeyAction`
- **`explorer_view.rs`**: Added `format_document()`, `rename_symbol()`, `request_code_actions()`, `apply_code_action()`, `request_document_symbols()` methods + popup state fields

---

## Phase 10: Terminal Integration -- COMPLETE (10.1-10.2)

### Phase 10.1 -- Editor + Terminal Split Layout
- **Cmd+`** toggles a terminal panel below the editor when in Explorer view
- `terminal_panel_open` flag + `terminal_panel_ratio` (default 35%) on `UiState`
- egui reserves bottom portion via `TopBottomPanel::bottom` with separator divider
- Terminal panes resize to lower portion via adjusted `content_rect()`
- `settings_open()` returns false when panel active so terminal rendering continues
- Toggling from Shells view auto-switches to Explorer

### Phase 10.2 -- Click-to-File from Terminal
- **Cmd+Click** on terminal text parses `file:line:col` patterns via regex
- Patterns: `src/main.rs:42:10`, `file.py:123`, `File "test.py", line 42` (Python tracebacks)
- Opens file in editor at matched position, auto-opens terminal panel + switches to Explorer
- Falls back to URL/hyperlink detection if no file pattern matches

### Changes
- `src/main.rs`: Added `parse_file_location()` with regex patterns, extended Cmd+Click handler, added `ToggleTerminalPanel` action with pane resize, modified `content_rect()` to account for panel
- `src/ui/mod.rs`: Added `terminal_panel_open`, `terminal_panel_ratio`, `toggle_terminal_panel()`, updated `settings_open()` logic, bottom panel reservation in Explorer rendering
- `src/keybindings.rs`: Added `ToggleTerminalPanel` action + Cmd+` binding

---

## Phase 9: File Management -- COMPLETE (9.1-9.3)

### Phase 9.1 -- File Tree Panel
- Replaced flat directory listing with recursive `TreeNode` structure
- Lazy child loading: children read from disk on first expand
- Click directory to expand/collapse; click file to open in editor
- File type icons: R (Rust), J (JS), T (TS), P (Python), G (Go), C (C), { (JSON), etc.
- Auto-filters: .git, node_modules, target, __pycache__, venv, dist, build, .next, .cache, hidden files
- Nested toggle via index-path tracking for arbitrary tree depth

### Phase 9.2 -- Fuzzy File Finder (Cmd+P)
- Opens from editor (Cmd+P) or "Find File" button in tree header
- Lazy-cached file index built by recursively collecting all files
- Fuzzy subsequence matching on file name and full path
- Up/Down navigation, Enter to open, Escape to dismiss
- Capped at 30 results; file type icons and relative path shown

### Phase 9.3 -- Project Detection
- `ExplorerState::set_root(path)` rebuilds tree at project root
- Project name displayed in tree header
- Reuses `LspManager::detect_root()` markers

### Changes
- `src/explorer.rs`: Complete rewrite (315 lines) -- TreeNode structure, lazy loading, IGNORED_DIRS filter, fuzzy_match(), file index caching
- `src/ui/explorer_view.rs`: Added tree rendering (render_tree_nodes, render_tree_children, toggle_at), fuzzy finder overlay (render_finder), file_icon(), TreeAction enum
- `src/editor/keymap.rs`: Added `open_file_finder` to KeyAction, Cmd+P binding

---

## Phase 11: Productivity UX -- COMPLETE (11.1, 11.2, 11.5)

### Phase 11.1 -- Command Palette (Cmd+Shift+P)
- Overlay popup with 26 registered commands and keybinding display
- Fuzzy subsequence filtering, Up/Down/Enter/Escape navigation
- Returns `CommandId` for execution by the host
- Dark themed with rounded frame, 400px wide, centered near top

### Phase 11.2 -- Minimap
- 50px-wide overview strip on right edge of editor (replaces simple scrollbar)
- Viewport indicator showing visible range as translucent blue rect
- Per-line colored density rendering from syntax highlight data
- Sampled for performance (max 1000 points for files >2000 lines)
- Diagnostic markers: red/yellow dots for errors/warnings in minimap

### Phase 11.5 -- Breadcrumbs
- File path segments rendered above editor with `>` separators
- Dimmed styling, auto-generated from buffer file path

### Changes
- `src/ui/command_palette.rs`: New file (190 lines) -- Command, CommandId, PaletteState, render_palette, all_commands registry
- `src/ui/editor_view.rs`: Added minimap rendering (density + diagnostics + viewport), breadcrumbs bar
- `src/ui/mod.rs`: Added palette state, Cmd+Shift+P toggle, palette_command output, registered command_palette module

---

## Phase 12: Performance -- COMPLETE (12.1)

- `src/editor/perf.rs` (33 lines): Centralized performance thresholds
- Syntax highlighting disabled for files >100k lines
- Minimap disabled for files >50k lines
- LSP `didChange` skipped for files >200k lines, debounced at 100ms
- Tree-sitter reparse gated by `syntax_enabled()` in `EditorState::reparse_active()`
- Minimap line sampling already capped at 1000 points

### Leftovers Roadmap Created
- `daily-growth/roadmaps/leftovers-code-editor.md`: All deferred tasks from Phases 2-12 plus Phase 13 (Distribution), organized by original phase

---

## UI Restructure -- Home Screen, Footer Nav, Sidebar File Tree

### Home Screen
- App opens to a centered Home screen: llnzy title, subtitle
- **Terminal** button (blue) -- opens the shell
- **Open Project** button (green) -- native folder picker, sets project root, opens sidebar with file tree, switches to Explorer view
- **Workspace** button (greyed) -- "Coming soon"
- **Recent Projects** section -- last 5 projects, persisted to `~/.config/llnzy/recent_projects.json`, click to reopen

### Footer Navigation Bar
- Moved all view navigation buttons from sidebar to footer
- Compact horizontal layout: Home | Terminal | Stacker | Sketch | Appearances | Settings
- Active view highlighted, click to switch

### Sidebar File Tree
- Sidebar now shows the project file tree (not navigation buttons)
- Project name header, scrollable tree with expand/collapse
- Clicking a file opens it in the main editor area
- **Bumper**: when sidebar is closed, a thin 12px strip remains on the left edge -- click to reopen
- Cmd+B still toggles sidebar
- Empty state: "Open a project from the Home screen"

### Main Editor Area
- Classic code editor layout: sidebar = file tree, main area = tabbed editor
- When no files open: centered hint "Open a file from the sidebar / Cmd+P to find a file"
- Cmd+P fuzzy finder renders as overlay in the main area

### Large File Stability
- File index for fuzzy finder now uses iterative BFS walk (no recursion) capped at 10,000 files
- Skips ignored dirs during walk itself, not after loading
- No tree mutation during indexing -- reads filesystem directly

### Mouse Scroll Support
- Mouse wheel scrolling in the editor (scroll delta / line height)
- Click-and-drag to select text
- `pixel_to_editor_pos()` helper for consistent mouse-to-editor coordinate mapping

### Changes
- `src/ui/mod.rs`: Footer with nav buttons, sidebar with file tree + bumper, Home screen with Open Project + Recent Projects, sidebar_open state tracked through render closure
- `src/ui/explorer_view.rs`: Added `render_sidebar_tree()` for sidebar, fuzzy finder moved to overlay in Explorer view, "no files open" hint
- `src/ui/editor_view.rs`: Mouse wheel scroll handling, click-and-drag selection, `pixel_to_editor_pos()` helper
- `src/explorer.rs`: `walk_files_capped()` iterative BFS with 10k cap, `load_recent_projects()` / `save_recent_projects()` / `add_recent_project()` / `project_name()` persistence functions
- `src/main.rs`: Open Project handler sets explorer root + opens sidebar, Home view in effects_on_ui match
- `src/ui/types.rs`: Added `ActiveView::Home`
- `src/keybindings.rs`: Added `ToggleTerminalPanel` action

---

## Sidebar Bumper Overhaul & UI Modularization (Session 2)

### Sidebar Bumper
- Bumper strip always visible on left edge (20px) with directional chevrons
- Closed state: `>>` chevron, click to open sidebar
- Open state: `<<` chevron on right edge of sidebar, click to close
- Hover feedback: chevron turns white, pointer cursor
- `BUMPER_WIDTH` constant in `types.rs` for consistent use across codebase

### Terminal Layout Fix -- No Underlap
- `sidebar_width()` now returns `BUMPER_WIDTH` (20px) when closed instead of `0.0` -- terminal content always stops at the bumper edge
- Initial startup layout uses `BUMPER_WIDTH` instead of `0.0`
- Added sidebar state change detection after egui render: snapshots `sidebar_width()` before/after, triggers `recompute_layout()` + pane resize on change
- Fixed Open Project also triggering layout recompute when it opens the sidebar
- Tab bar click-position math uses `BUMPER_WIDTH` for closed-state offset

### Sidebar Toggle Bug Fix
- Fixed inverted logic in `render_sidebar()` where `!render_bumper()` was closing the sidebar every frame the close button wasn't clicked (caused snap-open/snap-close behavior)

### Single-Click Footer Navigation Fix
- Removed `settings_open() || sidebar_open` guard from egui event consumption -- all egui-consumed events are now respected regardless of active view
- Previously, clicking footer buttons on the Terminal view with sidebar closed required double-click because the first click leaked through to the terminal

### UI Modularization (947 -> 587 lines in mod.rs)
Extracted four new modules from `src/ui/mod.rs`:

| File | Lines | Responsibility |
|------|-------|----------------|
| `src/ui/sidebar.rs` | 116 | File tree panel + bumper (open/close chevrons) |
| `src/ui/footer.rs` | 59 | Bottom navigation bar |
| `src/ui/home_view.rs` | 143 | Home screen (Terminal/Open Project/Workspace buttons, recent projects) |
| `src/ui/overlays.rs` | 209 | Tab bar rename interaction, copy ghosts, command palette, FPS overlay |

Each module has a clean function-based API -- takes state in, returns actions/results.

### Files Modified
- `src/ui/types.rs`: Added `BUMPER_WIDTH = 20.0`
- `src/ui/mod.rs`: Extracted sidebar/footer/home/overlays, fixed `sidebar_width()` to always include bumper
- `src/main.rs`: Added `BUMPER_WIDTH` import, startup layout uses bumper width, sidebar state change detection after egui render, simplified event consumption guard

### Files Created
- `src/ui/sidebar.rs` (116 lines)
- `src/ui/footer.rs` (59 lines)
- `src/ui/home_view.rs` (143 lines)
- `src/ui/overlays.rs` (209 lines)

---

## Unified Tabbed Workspace Refactor (Session 3)

Major architectural refactor: replaced the "terminal behind everything" model with a unified tabbed workspace where every content type is a tab.

### Architecture Change
- **Before**: Terminal always rendered by wgpu as background layer. Egui views (Home, Stacker, Sketch, etc.) overlaid on top. Split panes within terminal tabs.
- **After**: Main content area is a universal tabbed workspace. Each tab can be Terminal, CodeFile, Stacker, Sketch, Appearances, or Settings. No split panes. Home is the only overlay view.

### New Data Model (`src/workspace.rs`)
- `TabContent` enum: `Terminal(Box<Session>)`, `CodeFile { path, buffer_idx }`, `Stacker`, `Sketch`, `Appearances`, `Settings`
- `WorkspaceTab` struct with `content`, `name`, `id`
- `TabKind` discriminant enum for borrow-free matching
- `find_singleton()` helper for singleton tab lookup

### Split Panes Removed
- Removed `PaneNode`, `SplitDir`, `split_pane()`, `split_rect()`, divider drag, cycle-pane from `session.rs` (393 lines deleted, 559→166)
- `Cmd+D` now creates a new terminal tab inheriting CWD
- All divider drag detection/handling removed from mouse events

### Renderer Refactored
- `RenderRequest` takes `Option<&Session>` + `tab_id` instead of `&PaneNode`
- Single-session rendering (no pane tree traversal, no dividers)
- Non-terminal tabs skip wgpu content, egui handles rendering
- Text cache key changed from pointer to tab ID

### Egui Tab Bar
- Replaced wgpu-rendered tab bar with egui `TopBottomPanel::top`
- Each tab shows name + clickable "x" close button
- Active tab highlighted in blue, hover cursor on close button
- Tab switching and closing handled via `TabBarAction` signal to main loop

### Everything Is a Tab
- Terminal, CodeFile, Stacker, Sketch, Appearances, Settings all open as workspace tabs
- Footer buttons: Home (overlay), Terminal (new tab), Stacker/Sketch/Appearances/Settings (singleton tabs)
- Closing the last tab returns to Home screen
- No terminal spawned at startup -- created on demand via footer or Cmd+T

### Code Editor Cleanup
- Removed breadcrumb path bar from editor view
- Removed internal editor tab bar and "< Files" back button
- Code files render directly in the workspace tab content area
- Sidebar file click creates a `CodeFile` tab (deduplicates by path)

### Sidebar Improvements
- Sidebar is now resizable (drag right edge, 140px–400px range)
- Background fills dynamically with panel width -- no gaps
- `sidebar_width()` returns actual panel width, not a fixed constant

### macOS Menu Bar
- Added "Open Project..." (Cmd+O) to File menu -- native folder picker
- Added "Close Project" to File menu -- clears project, closes tabs, returns to Home

### Terminal CWD Inheritance
- New terminal tabs inherit CWD from active terminal session
- Falls back to project root if no terminal is active
- Falls back to home directory if no project is open

### Terminal Padding
- Increased default padding: `padding_x: 20px`, `padding_y: 70px`

### Input Routing
- All keyboard/mouse input guarded by active tab type
- Terminal-specific input (PTY writes, mouse reporting, selection, search) only sent to terminal tabs
- IME and drag-and-drop also guarded

### Key Stats
- `session.rs`: 559→166 lines (−70%)
- `main.rs`: 1543→~1400 lines (−9%)
- `renderer/mod.rs`: simplified single-session rendering
- New files: `workspace.rs` (91 lines), `ui/tab_bar.rs` (100 lines)
- Net: −460 lines across the codebase
- All 320 tests pass

### Files Created
- `src/workspace.rs` -- TabContent, WorkspaceTab, TabKind, find_singleton
- `src/ui/tab_bar.rs` -- egui tab bar rendering (unused, inline approach used instead)

### Files Modified
- `src/main.rs` -- WorkspaceTab adoption, split pane removal, input routing, footer/tab bar actions, menu handlers
- `src/session.rs` -- PaneNode/SplitDir/split_pane removed, only Session + Rect remain
- `src/renderer/mod.rs` -- RenderRequest refactor, single-session rendering, wgpu tab bar disabled
- `src/renderer/text.rs` -- tab_x_offset parameter for label positioning
- `src/ui/mod.rs` -- Overlay system, tab-kind-based content rendering, resizable sidebar width tracking
- `src/ui/footer.rs` -- FooterAction enum, Terminal/singleton tab buttons
- `src/ui/sidebar.rs` -- Resizable panel, SidebarResult return type
- `src/ui/explorer_view.rs` -- Removed internal tab bar/back button/breadcrumbs, pending_file_tab signal
- `src/ui/editor_view.rs` -- Removed breadcrumb path bar
- `src/ui/home_view.rs` -- Open Project dismisses to Shells view
- `src/ui/overlays.rs` -- Tab bar guard updated for new view system
- `src/explorer.rs` -- Added clear() method
- `src/menu.rs` -- Added OpenProject/CloseProject menu actions
- `src/config.rs` -- Updated default padding values
- `src/lib.rs` -- Registered workspace module

---

## Leftovers Roadmap Execution (Session 4)

Systematic implementation of deferred items from `leftovers-code-editor.md`. Planned all 16 remaining items into 5 priority tiers, then executed Tiers 1-2 (6 items).

### 3.7 -- Find & Replace -- COMPLETE

New file: `src/editor/search.rs` (280 lines, 8 tests)
- `EditorSearch` struct: query, replacement, match list, focus index, dirty flag
- Modes: plain text (case-insensitive default), case-sensitive, whole-word, regex
- `update_matches(buf)`: scans rope buffer line-by-line, supports overlapping/non-overlapping
- `next()` / `prev()`: cycle through matches with wrap-around
- `replace_current(buf)`: replace focused match, re-scan, advance
- `replace_all(buf)`: replace in reverse order to preserve positions
- `focus_nearest(pos)`: jump to nearest match after cursor

UI: Find/replace bar rendered at top of editor area via `render_search_bar()` in `editor_view.rs`
- Query + replacement TextEdit inputs
- Toggle buttons: Aa (case), W (whole word), .* (regex) -- blue when active
- Nav buttons: < (prev), > (next), status "3/15"
- Replace / Replace All buttons (only in replace mode)
- Keyboard: Escape closes, Enter next, Shift+Enter prev
- Match highlighting: orange-ish background rects, focused match brighter

Keybindings: Cmd+F (find), Cmd+H (find & replace)
Command palette: `Find`, `Find and Replace` added

### 4.3 -- Save Prompts -- COMPLETE

- `PendingClose` enum in `ui/types.rs`: `Tab(index, name)` | `Window(Vec<(index, name)>)`
- `SavePromptResponse` enum: `Save` | `DontSave` | `Cancel`
- Modal dialog rendered via `render_save_prompt()` in `ui/overlays.rs`
  - Dimmed background overlay, centered dialog with title + body + 3 buttons
  - Keyboard: Escape = Cancel, Enter = Save
- `close_tab()` in main.rs checks `is_modified()` on CodeFile buffers before closing
- `CloseRequested` scans all tabs for unsaved buffers
- `force_close_tab()` extracted for unconditional close after dialog response
- Save response handler: saves buffer (or save-as via rfd if no path), then closes/exits

### 11.3 -- Git Gutter Indicators -- COMPLETE

New file: `src/editor/git_gutter.rs` (280 lines, 5 tests)
- `GitGutter::load(path)`: runs `git show HEAD:<relative-path>` to get committed version
- `recompute(current_lines)`: LCS-based diff producing `GutterHunk` list
  - Standard DP LCS for files up to ~3k lines, fast greedy LCS for larger files
  - Hunks classified as Added, Modified, or Deleted
- `change_at(line)`: O(hunks) lookup for a specific line
- Debounced at 500ms to avoid lag during rapid typing
- `mark_dirty()` called after buffer edits

Rendering in `editor_view.rs`:
- Green bar (3px) for added lines
- Blue bar for modified lines
- Red bar (4px) for deleted lines (positioned between lines)
- Bars drawn at right edge of gutter, before fold indicators

Loaded in `EditorState::open()`, stored as `Option<GitGutter>` in `BufferView`

### 7.5 -- Find References -- COMPLETE

- `client.rs`: Added `async fn references()` sending `textDocument/references` with `include_declaration: true`
- `mod.rs`: `ReferenceLocation { path, line, col, context }` struct, blocking `references()` wrapper that reads context lines from disk
- `keymap.rs`: `find_references` flag on `KeyAction`, bound to Shift+F12
- `explorer_view.rs`: `references_popup` + `references_selected` state, `request_references()` method, egui `Window` rendering with scrollable list grouped by file:line, click/Enter to navigate, Escape to dismiss
- Command palette: `FindReferences` added

### 7.6 -- Signature Help -- COMPLETE

- `client.rs`: Added `async fn signature_help()` sending `textDocument/signatureHelp`
- `mod.rs`: `SignatureInfo { label, parameters, active_parameter }` struct, blocking wrapper that extracts active signature and resolves `ParameterLabel::LabelOffsets`
- Trigger: automatically requested when `(` or `,` is typed (detected via `char_at()` after edit), dismissed on `)`
- Rendering in `editor_view.rs`: floating tooltip above cursor showing full signature label with the active parameter highlighted in yellow (#FFDC64), rest dimmed

### 8.5 -- Workspace Symbol Search -- COMPLETE

- `client.rs`: Added `async fn workspace_symbols(query)` sending `workspace/symbol`
- `mod.rs`: `WorkspaceSymbol { name, kind, path, line, col }` struct, blocking wrapper
- `keymap.rs`: `workspace_symbols` flag on `KeyAction`, bound to Cmd+Shift+T
- `explorer_view.rs`: `workspace_symbols_popup` + query + selected state, `request_workspace_symbols()` method, egui `Window` with search input (re-queries LSP on each keystroke), scrollable results showing name + kind + file:line, click/Enter to navigate, Escape to dismiss
- Command palette: `WorkspaceSymbols` added

### Test Results

**356 tests passing** (up from 320 at start of session).

| New Tests | Count |
|-----------|-------|
| `editor::search` | 8 |
| `editor::git_gutter` | 5 |
| **Net new** | **13** |

### Files Created

| File | Lines | Description |
|------|-------|-------------|
| `src/editor/search.rs` | 280 | Editor find & replace engine with regex support |
| `src/editor/git_gutter.rs` | 280 | Git diff gutter with LCS algorithm |

### Files Modified

| File | Changes |
|------|---------|
| `src/editor/mod.rs` | Added `search`, `git_gutter` modules; `git_gutter` field on `BufferView`; load gutter in `open()` |
| `src/editor/keymap.rs` | Added `open_find`, `open_find_replace`, `find_references`, `workspace_symbols` to `KeyAction`; Cmd+F, Cmd+H, Shift+F12, Cmd+Shift+T bindings |
| `src/lsp/client.rs` | Added `signature_help()`, `references()`, `workspace_symbols()` async methods |
| `src/lsp/mod.rs` | Added `SignatureInfo`, `ReferenceLocation`, `WorkspaceSymbol` types; blocking wrappers for all three |
| `src/ui/editor_view.rs` | Added `EditorSearch` + `SignatureInfo` parameters to `render_text_editor()`; search bar UI, match highlighting, git gutter bars, signature help tooltip |
| `src/ui/explorer_view.rs` | Added `editor_search`, `references_popup`, `signature_help`, `workspace_symbols_popup` to `EditorViewState`; request methods; popup rendering; edit triggers for signature help + search dirty |
| `src/ui/command_palette.rs` | Added `Find`, `FindReplace`, `FindReferences`, `WorkspaceSymbols` command IDs |
| `src/ui/overlays.rs` | Added `render_save_prompt()` modal dialog |
| `src/ui/types.rs` | Added `PendingClose`, `SavePromptResponse` enums |
| `src/ui/mod.rs` | Added `pending_close`, `save_prompt_response` to `UiState`; exported new types; save prompt rendering in egui closure |
| `src/main.rs` | Modified `close_tab()` for unsaved buffer check; `CloseRequested` for window close check; save prompt response handler with `force_close_tab()` |
| `daily-growth/roadmaps/leftovers-code-editor.md` | Checked off 3.7, 4.3, 7.5, 7.6, 8.5, 11.3 |

---

## What's Next

- Remaining deferred items tracked in `daily-growth/roadmaps/leftovers-code-editor.md`
- **Tier 3**: Inlay hints (8.6), Code lens (8.7)
- **Tier 4**: Async LSP (12.2), File watching (4.4), Multi-file search (9.4)
- **Tier 5**: Snippet engine (11.4), Run tasks (10.3), Keybinding presets (13.1), Profiling (12.4)
- **Deferred**: Extension API (13.2), GPU text migration (12.3), Cross-platform testing (13.3)
