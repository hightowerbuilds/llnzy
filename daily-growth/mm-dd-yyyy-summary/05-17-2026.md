# 05-17-2026 Summary

## Breadcrumbs

Top-down scan of everything done today, in order.

**Codebase orientation**
- Read the workspace entry points, core modules, GPUI surfaces, terminal/editor
  layers, Stacker, Sketch, config, LSP, recovery, and packaging flow.
- Confirmed LLNZY is a Rust/macOS GPUI desktop workbench centered on terminal,
  editor, project/sidebar, Stacker, Sketch Pad, appearances, tabs, diagnostics,
  and release packaging.

**Roadmap cleanup and shortlist**
- Rewrote `daily-growth/roadmaps/shortlist.md` into a clear six-item checklist.
- Added the discovered rename-file bug to the bottom of the shortlist.
- Roadmap folder cleanup in the worktree moved older roadmap notes under
  `daily-growth/roadmaps/old/` and kept current/future work separated.

**Shortlist item 1: Cmd+B sidebar toggle**
- Added the global workspace keybinding for `Cmd+B`.
- Routed keyboard and menu behavior through the same sidebar toggle command.
- Kept previous sidebar width restoration intact.

**Shortlist item 2: Swap Tabs tab-bar ordering**
- Fixed joined-tab swapping so the visual tab-bar order changes with the pane
  order.
- Extracted `place_workspace_tabs_together` and added a focused tab-order test.
- Preserved joined-pane state and active-tab behavior.

**Shortlist item 3: Terminal display mode width**
- Added separate display-mode PTY sizing via `display_advance`.
- Monospace mode still uses the fixed terminal grid advance.
- Display mode now allocates columns from a natural average text probe while
  cursor, selection, decorations, and mouse hit testing still use shaped row
  offsets.
- Added a focused terminal grid sizing regression test.

**Shortlist item 4: Home as a real closable tab**
- Removed the forced Home recreation path after the last tab closes.
- Added a real empty workspace state with centered `llnzy` text over the open
  background.
- Footer/menu Home opening still creates or activates Home as expected.

**Shortlist item 5: editor desktop menu audit**
- Compared desktop menu items, command palette entries, workspace action
  handlers, and editor-side methods.
- Fixed desktop Paste so it respects active editor overlays like Find and LSP
  Rename instead of bypassing them.
- Added clearer status feedback for unavailable editor actions with no active
  buffer or unavailable LSP rename context.

**Shortlist item 6: rename-file bug**
- Fixed sidebar rename failure paths so failed Save attempts restore the rename
  state instead of leaving the UI in Rename view with no backing state.
- Rename failures now keep showing a clear status message and allow another
  Save attempt.
- Successful renames still remap open file tabs and editor buffers only after
  the filesystem rename succeeds.

**Settings editor word wrap**
- Added a persisted Settings > Editor word-wrap toggle backed by workspace
  preferences.
- Applied that preference across the shared editor config and language-specific
  editor overrides so JavaScript, Markdown, and other long-line text files all
  follow the same setting.
- Added wrapped visual-row rendering for source files, including continuation
  gutter rows, clipped line segments, cursor visibility, selection painting,
  search matches, diagnostics, and ruler suppression while wrap is active.
- Added wrapped-row hit testing, drag selection, vertical scrolling, cursor
  reveal, viewport resize handling, and Up/Down/PageUp/PageDown movement that
  operates on wrapped visual rows instead of only logical lines.
- Added per-buffer wrapped scroll state so classic horizontal scrolling remains
  intact when word wrap is off.
- Fixed a wrapped-line crash discovered while joining multiple code files by
  making cursor segment detection avoid off-segment column subtraction.

**Word wrap crash follow-up**
- Investigated a macOS crash report from joining multiple code files, with the
  panic landing in `gpui_editor::line_render::editor_line`.
- Traced the underflow to eager `then_some(...)` evaluation building a cursor
  position before confirming the cursor belonged to the rendered wrapped
  segment.
- Added regression coverage for cursors before a wrapped segment, inside a
  wrapped segment, and at the final wrapped line end.

## Verification

- `cargo fmt --check`
- `cargo check --all-targets --all-features`
- `cargo test gpui_workspace::tabs::tests::tab_`
- `cargo test gpui_terminal::render::tests::grid_size_uses_display_advance_only_for_display_layout`
- `cargo test gpui_editor::input::tests::wrapped --quiet`
- `cargo test gpui_editor::line_render::tests::visible_cursor --quiet`
- `cargo test preferences --quiet`
- `cargo test --all-targets --all-features --quiet`
- `git diff --check`
