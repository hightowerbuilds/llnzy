# 05-11-2026 Summary

## Mainline cleanup and GPUI direction

Started from the May 10 pickup point: Stacker native input work had been
validated, but the work still needed to be landed cleanly on `main`.

Completed that cleanup:

- Pushed the remaining local work through `main`.
- Fast-forwarded / merged the GPUI spike history into `main`.
- Removed the unneeded spike branch after it had served its purpose.
- Kept the GPUI work feature-gated so the current app remains runnable.

Relevant commits now on `main`:

- `f30ff39` — checkpointed remaining local changes before the main merge.
- `e2c7440` — added the feature-gated GPUI Stacker prototype.
- `fdde381` — advanced GPUI editor and Stacker prototypes.

## GPUI spike result

The standalone GPUI spike proved enough to proceed:

- GPUI builds on this machine with the pinned `=0.2.2` dependency and the
  `macos-blade` feature, avoiding a full Xcode requirement.
- A standalone GPUI window, layout, text entry path, clipboard/key actions,
  and custom drawing path all compile and run.
- Wispr Flow worked for text entry in the GPUI spike.
- Mouse interaction worked for text input; the tab labels in the spike were
  not wired as interactive controls, so their non-click behavior was not a
  meaningful blocker.

Decision: GPUI has enough proof to begin the migration, starting with
Stacker and the code editor. Terminal migration remains explicitly out of
scope.

## Roadmap rewrite

Rewrote `daily-growth/roadmaps/gpui2-modular-integration.md` from the
ground up around a spike-first and surface-by-surface migration plan.

Current scope:

- **First:** Stacker.
- **Second:** code editor.
- **Later:** Explorer/sidebar.
- **Not this wave:** terminal rendering, PTY/session ownership, terminal
  `wgpu` effects, or full event-loop replacement.

The roadmap now also includes branch discipline: use a dedicated spike branch,
push milestones early, commit each proof separately, and merge only after exit
criteria are satisfied.

## GPUI Stacker prototype

Added `src/gpui_stacker.rs` and `src/bin/gpui_stacker.rs` behind:

```toml
gpui-stacker = ["dep:gpui"]
```

The prototype uses real Stacker data:

- Loads saved prompts.
- Shows a prompt list.
- Opens a GPUI prompt editor.
- Supports keyboard editing, selection, clipboard, undo/redo, mouse focus, and
  GPUI input-handler integration.

Follow-up in `fdde381`: replaced the initial newline-flattening workaround with
real multiline prompt rendering. GPUI's low-level `shape_line` is single-line,
so the prototype now shapes each prompt line independently and maps cursor,
selection, mouse hit-testing, and IME bounds through that multiline layout.

Known limitation: no wrapping or scrolling yet. This is acceptable for the
prototype stage.

## GPUI editor prototype

Added a separate editor prototype behind:

```toml
gpui-editor = ["dep:gpui"]
```

Files:

- `src/gpui_editor.rs`
- `src/bin/gpui_editor.rs`

The first editor pass is intentionally read-only. It opens a real
`EditorState`, loads an initial file when available, reparses the active
buffer, and renders a GPUI editor surface with line numbers, file metadata,
language label, and status text.

This gives us a low-risk editor beachhead without touching the current editor
runtime or terminal path.

## Verification

After the GPUI prototype work:

```sh
cargo check
cargo check --features gpui-stacker --bin gpui-stacker
cargo check --features gpui-editor --bin gpui-editor
```

All passed.

Baseline `cargo check` still emits the pre-existing warning:

- `append_text_to_stacker_editor` is never used in `src/runtime/terminal.rs`.

## Later GPUI migration progress

Advanced the GPUI migration well past the earlier read-only/editor-prototype
state:

- Added wrapped/scrolling Stacker prompt editing in GPUI.
- Turned the GPUI editor into an editable `EditorState` surface with typing,
  deletion, enter, tab/shift-tab, keyboard navigation, selection, clipboard,
  undo/redo, save, mouse placement/drag selection, and syntax highlighting.
- Rewrote `daily-growth/roadmaps/gpui2-modular-integration.md` into a current
  migration tracker instead of an early spike checklist.
- Added a shared GPUI workspace shell behind:

```toml
gpui-workspace = ["gpui-editor", "gpui-stacker"]
```

New files:

- `src/gpui_workspace.rs`
- `src/bin/gpui_workspace.rs`

The workspace mounts the GPUI Stacker and GPUI editor in one GPUI process with
shared workspace chrome. The terminal remains explicitly absent.

Latest GPUI workspace verification:

```sh
cargo check --features gpui-workspace --bin gpui-workspace
cargo check --features gpui-stacker --bin gpui-stacker
cargo check --features gpui-editor --bin gpui-editor
cargo check
```

All passed. Baseline `cargo check` still emits only the existing
`append_text_to_stacker_editor` warning in `src/runtime/terminal.rs`.

## Appearances background image guard

Added a stability guard for terminal background images in the Appearances page.
The previous flow copied any selected image into the background library and the
renderer later decoded/uploaded it with no size or dimension limit, so very
large images could crash the app.

Implemented the fix in two layers:

- `theme_store::import_background` now validates images before copying them
  into the background library.
- The renderer now reuses the same validation before decoding/uploading an
  image, which protects existing saved backgrounds and hand-edited config paths.

Current limits:

- 50 MiB maximum source file size.
- 8192 px maximum width or height.
- 24 megapixels maximum total pixel count.

The Appearances UI now keeps and displays the import failure message inline
instead of only logging it, so users get a clear reason when an image is
rejected.

Verification:

```sh
cargo check
```

Passed. It still emits the pre-existing `append_text_to_stacker_editor`
warning in `src/runtime/terminal.rs`.

`cargo test --lib background_` was attempted during final push prep, but the
local test build failed at the linker with unresolved symbols, which appears to
be a local build-artifact/toolchain issue rather than a Rust typecheck failure.
The changed Rust code still passes `cargo check`; rerunning the focused test
from a clean target directory is the next verification step.

## End state

- Branch: `main`
- Remote: `origin/main`
- Latest GPUI migration commit: `b680b95 Restore GPUI workbench shell styling`
- Working tree before this summary/background commit contained the Appearances
  background-image guard changes and this summary update.

## Pickup point

Next migration slice:

1. Inspect the GPUI workspace shell on desktop.
2. Rename/refine the Stacker and editor prototype roots into reusable surface
   APIs.
3. Split editor construction so embedded workspace use does not depend on
   `env::args()`.
4. Decide whether child prototype headers stay inside the surfaces or move into
   workspace-owned chrome.
4. Keep the terminal untouched.
