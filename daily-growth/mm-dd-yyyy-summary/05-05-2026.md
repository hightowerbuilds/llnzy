# 05-05-2026 Summary

## Git Laundry List

Completed the Git section of the laundry list as a read-only, safety-first pass.

Completed:

- Dispatched parallel audits for Git UI/workflow coverage and Git backend/reliability coverage.
- Shared repository discovery by moving the editor Git gutter onto `git::discover_repo_root`.
- Kept Git tab command execution on background refresh/detail workers, with stale refresh identity now including log options.
- Preserved the existing CLI implementation behind the Git model/parser boundary so a future `git2` backend can fit behind the same public data model.
- Added Git log options for all-branches/current-branch scope, first-parent history, and active-editor-file history.
- Added Git UI controls for all-branches, first-parent, and active-file history.
- Wired Git working-tree rows and commit-detail file rows to open existing files through the normal editor tab flow.
- Added a debounced recursive repository watcher that triggers Git refreshes after repository or working-tree changes.
- Added `git worktree list --porcelain` parsing and Worktrees display support.
- Added keyboard commit navigation with Up, Down, Home, End, Enter, and Space.
- Kept mutating Git actions deferred until an explicit confirmation, command-preview, and failure-recovery UX exists.
- Marked the Git laundry-list section complete.

Validation:

- `cargo fmt --check`
- `cargo check`
- `cargo test git --lib`
- `cargo test`

## Rendering Performance And Graphics

Started the rendering performance section with a testable performance contract.

Completed:

- Added explicit budgets for terminal idle/output, editor large-file/word-wrap/diagnostics, effects, LSP, and Git refresh scenarios.
- Added repeatable harness definitions plus smoothness evaluation for average, p95, max, and missed-frame ratio.
- Added adaptive quality state with over-budget degradation, stable-frame recovery, and battery power quality caps.
- Wired the renderer to reduce expensive effects when recent frame timing exceeds the active budget.
- Added platform power-source detection and periodic app-to-renderer power updates so battery mode caps expensive quality recovery.
- Added clean/effects frame-smoothness tracking so effects-on and effects-off render paths are measured separately.
- Marked the completed rendering performance foundation items in the laundry list.

Validation:

- `cargo fmt --check`
- `cargo check`
- `cargo test power --lib`
- `cargo test performance --lib`
- `cargo test`

## Background Settings Cleanup

Picked up the next concrete Rendering, Performance, and Graphics laundry-list item.

Completed:

- Made Settings derive built-in background shader names from the renderer registry instead of duplicating the list.
- Kept the default built-ins to `smoke` and `aurora`; `matrix`, `nebula`, and `tron` remain removed from the default UI unless supplied as custom `.wgsl` shaders.
- Normalized unavailable configured background modes back to `none` so Settings does not present dead shader choices.
- Added focused tests for built-in registry drift, custom shader mode listing, image-mode availability, and unavailable-mode normalization.
- Marked the background option mismatch and `matrix`/`nebula`/`tron` decision items complete in the laundry list.

Validation:

- `cargo fmt --check`
- `cargo test background --lib`

## Custom Shader Loading Verification

Completed:

- Moved custom `.wgsl` shader discovery into the renderer background module so Settings and renderer loading share the same file filtering, sorting, and file-stem naming rules.
- Verified custom shader discovery ignores non-WGSL files, sorts shader names deterministically, and prefixes custom fragments with the shared renderer preamble before pipeline compilation.
- Kept custom shaders available in Settings through the same renderer-owned discovery helper.
- Marked the custom shader loading verification item complete in the laundry list.

Validation:

- `cargo fmt --check`
- `cargo test background --lib`

## Stacker And External Input

Revisited the Stacker laundry-list section with parallel audits.

Completed:

- Added current Stacker command, text-mutation, saved-prompt, manual verification, external-input trace, IPC, shell automation, and code-editor handoff notes in `docs/stacker-command-workflow-05-05-2026.md`.
- Kept manual-only Stacker verification in `daily-growth/roadmaps/manual laundry.md` instead of treating live typing, paste, latency, dictation routing, toolbar selection, and saved-prompt modal behavior as code-only closure.
- Changed Stacker formatting shortcut handling to derive from `stacker_command_registry()` instead of duplicating hardcoded command IDs.
- Added focused `LLNZY_TRACE_EXTERNAL_INPUT` flag parsing tests.
- Marked the code-backed and doc-backed Stacker section items complete in `daily-growth/roadmaps/laundry-list.md`.

Validation:

- `cargo fmt --check`
- `cargo test stacker --lib`
- `cargo test external_input_trace --lib`
- `cargo test command_palette --lib`
- `cargo test stacker_format_shortcuts`
- `cargo test external_commands`
- `git diff --check`
- `cargo check`

## Time-Of-Day Warmth Clarification

Completed:

- Changed `apply_time_of_day` from accidental UTC-day math to explicit local wall-clock hour behavior.
- Added deterministic tests for midday cool shift, night warm shift, and neutral dawn/dusk boundaries.
- Added `[colors].time_of_day_enabled` config parsing so the existing runtime option can be persisted in `config.toml`.
- Documented that time-of-day warmth uses local computer time, not UTC.
- Marked the local-vs-UTC clarification item complete in the laundry list.

Validation:

- `cargo fmt --check`
- `cargo test config --lib`
- `cargo check`

## Terminal Rendering Visual Verification Coverage

Completed:

- Added a focused manual verification matrix for stale terminal text artifacts after scroll, split resize, tab switch, theme change, and effects/background changes.
- Added terminal text correctness coverage for resize, scrollback, splits, tabs, themes, ANSI attributes, wrapping, selection, and copied text.
- Included explicit setup, scenario actions, pass criteria, a combined high-risk flow, and evidence expectations for future live runs.
- Left both Rendering, Performance, and Graphics laundry-list items unchecked because this is documented manual verification coverage only, not an executed screenshot-tested pass.

Validation:

- Documentation-only change; no runtime tests were run for this coverage matrix.

## Rendering Architecture Foundation

Completed:

- Documented the current `RenderRequest -> EngineFrame -> legacy passes` migration boundary.
- Added effect masks to engine `EffectStack` and attached request masks through the frame adapter so post-processing consumes masks from `EngineFrame`.
- Added render-aligned `HitRegion` support to `EngineFrame`, plus terminal-content and egui hit regions in the frame adapter.
- Kept search bar and error panel text on first-class `LayerKind::Text` and documented terminal/editor text migration scope.
- Documented blur/color grading as deferred until layer-aware execution is broader.
- Documented editor GPU text and lazy long-line rendering as profiling-gated decisions.
- Added explicit text cache eviction on GPU out-of-memory.
- Marked the remaining Rendering, Performance, and Graphics architecture items complete at migration-foundation scope.

Validation:

- `cargo fmt --check`
- `cargo test engine --lib`
- `cargo test frame_adapter --lib`
- `cargo test renderer::passes --lib`
- `cargo check`

## Workspace And Theme UX Pass

Completed:

- Fixed the command palette `Open Workspace` action so it opens the Settings workspace switcher instead of the project folder picker.
- Confirmed the workspace Settings page already creates, launches, and deletes saved workspace bundles with theme, project, and tab entries.
- Confirmed Settings and Appearances are separate singleton product surfaces.
- Added background image library persistence tests for import, duplicate-name handling, sorted listing, and deletion.
- Added custom theme persistence tests for save/load/delete and stored per-view flags.
- Added clear Settings feedback for empty saved background lists and unavailable selected background image paths.
- Marked workspace settings, Home/palette workspace switcher, Settings/Appearances separation, background image library behavior, custom theme creation, and hot-switch animation deferral complete in the laundry list.
- Left workspace auto-save/restore unchecked because autosave still writes `theme = None`.
- Left Settings/Stacker render verification, full optional asset empty-state verification, background image changing, and per-view theme application unchecked pending live/manual verification or implementation.
- Added a direct regression test that verifies default primary-modifier zoom bindings for zoom in, zoom out, and reset.
- Corrected README product claims around built-in theme count, per-view theme behavior, workspace launching, and session theme autosave.
- Documented platform-specific `config.toml` paths and app-owned `themes/`, `workspaces/`, `backgrounds/`, and `shaders/` directories in the configuration reference.
- Marked Cmd+/Cmd- zoom verification, README/config docs audit, and built-in theme count/config path documentation complete in the laundry list.
- Left welcome/onboarding, keyboard shortcut cheat sheet overlay, notification toasts, and setup-with-appearance unchecked.

Validation:

- `cargo fmt`
- `cargo fmt --check`
- `cargo check`
- `cargo test palette_command_dispatch --lib`
- `cargo test theme_store --lib`
- `cargo test settings_tabs::background --lib`
- `cargo test keybindings --lib`
- `cargo test`

## Security And Governance Baseline

Completed:

- Added `docs/security-governance-05-05-2026.md` as the current security and governance baseline.
- Documented a threat model covering source files, terminal state, config/workspace/theme data, clipboard content, future extension state, and external command surfaces.
- Defined trust boundaries for terminals, project files, LSP processes, Stacker commands, custom assets, public IPC, and future plugins/extensions.
- Added dependency review, secure update chain, vulnerability handling, and secrets-handling policies.
- Defined the future enterprise policy model: managed settings, disabled features, enforced defaults, approved language servers/extensions/tools, telemetry policy, update controls, and profile locking.
- Clarified the current compatibility/support promise and kept enterprise deployment, auto-update, public IPC, and extension execution deferred.
- Marked the Security, Governance, and Enterprise Readiness laundry-list section complete at documentation/baseline scope.

Validation:

- Documentation-only change; no runtime tests were required.
